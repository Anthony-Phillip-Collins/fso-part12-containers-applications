{
  "name": "todo-express-backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "dev": "nodemon ./bin/www",
    "docker:dev": "docker compose -f docker-compose.dev.yml up --force-recreate",
    "docker:mongo:admin": "docker exec -it todo-backend-mongo-1 mongosh admin -u root -p 'example'",
    "docker:mongo:editor": "docker exec -it todo-backend-mongo-1 mongosh -u the_username -p 'the_password' the_database"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "debug": "~2.6.9",
    "dotenv": "^16.0.3",
    "express": "~4.16.1",
    "mongoose": "^5.13.2",
    "morgan": "~1.9.1",
    "redis": "^3.1.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}
